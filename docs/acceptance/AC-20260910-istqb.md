---
acceptance_id: AC-20260910-istqb
task_id: 20260910-istqb
intent_id: INT-MBB-001
owner: manual-bb-test-harness
status: completed
reviewed_at: 2026-09-10
reviewed_by: Codex
approval_type: null
release_approval_id: null
---

# 技法被覆の拡張・検証記録

## 対象

[Task Seed](../tasks/task-istqb-extension-20260910.md) と [調査採用表](../research/istqb-extension-adoption.md) に記載した有限モデルの実装。調査原本全要件の完了や実モデル品質の合格を表すものではない。

## 確認結果

- [x] 調査原本をMarkdownで保存し、SHA256が取得時と一致。
- [x] 同名・異なる入力のケースを保持し、riskの機械的接続を廃止。reviewによる必要ケースの消失を防止。
- [x] Domain、組み合わせ、状態経路、決定表、CRUD、scenario、checklistの有限被覆を検証。
- [x] randomの試行数とmetamorphicのjoint評価を実測入力・出力から確認。
- [x] 設計・実施・合格を分離。failも実施済み、未知の被覆IDは拒否。Gate判断はshadow追加前後で一致。
- [x] 型付きモデル、被覆義務、cases、証跡、reportをschema検証。root/package schemaが一致。
- [x] migrateは原本と既存出力を上書きせず、情報不足をneeds_review/unmappedとして保持。
- [x] Skill、ガイド、golden、example、評価基準、配布検証を同期。

## 自動検証

実行環境: Windows / Python 3.13.7。以下はrepoの `.venv/Scripts/python.exe` とruffを使用。

| 検証 | 結果 |
|---|---|
| 全体pytest | **835 passed**、138.06秒 |
| 全体カバレッジ（行・分岐を計測） | **86.16%**、85%基準を通過 |
| Gate専用pytest | **103 passed**、行・分岐を計測した総合カバレッジ **91.46%**、90%基準を通過 |
| 新coverage_engine単体 | 全体suiteで総合 **92.16%** |
| ruff | 全対象pass |
| artifact strict validation | 既存＋新規の28 artifactが有効 |
| spec validation | 既存4仕様pass |
| Skill検証 | repo版、system skill-creator版、PowerShell版すべてpass |
| 配布smoke | wheel/sdistを隔離環境へinstallし、既存CLIとcoverage/migrateがpass |
| Workflow Cookbook | 33 nodes / 45 edges / 33 capsules、835 testsで整合確認 |

主要コマンド:

```powershell
python -m pytest tests/ --cov=src/bb_harness --cov=scripts --cov-branch --cov-fail-under=85
python -m pytest tests/test_evaluate_gate.py tests/test_gate_v2.py --cov=bb_harness.gate_engine --cov-branch --cov-fail-under=90
ruff check src/ scripts/ tests/ tools/
python scripts/validate-artifact.py --all examples/artifacts --strict
python scripts/validate-artifact.py --all examples/coverage-evidence --strict
python scripts/validate-spec.py --all
python scripts/quick-validate-skill.py skills/manual-bb-test-harness
./scripts/validate-skill.ps1
python tools/ci/check_workflow_cookbook_freshness.py --repo . --strict
python tools/ci/package_smoke.py
```

package smokeは `PYTHONUTF8=1` を設定し、ビルド依存をPyPIから取得して実行した。検証対象は `bb_harness-3.0.0-py3-none-any.whl` と `bb_harness-3.0.0.tar.gz`。

追加exampleを既存Gate入力配下に置いた初回検証では、別featureの合成証跡を拒否する12件の回帰を検出した。証跡を `examples/coverage-evidence/discount-domain` へ移し、ZIPにも相対パスを保持させた。最終835件は修正後の結果。

## レビューしたサンプル

割引境界はD=2000円、P=12000/11999/12002/11997円をそれぞれON/OFF/IN/OUTとして保持する。合成証跡pass/fail/passの3件から、設計4/4、実施3/4、合格2/4となることを確認。全ケースが同じタイトルでも4入力は残る。

## 制約とフォローアップ

- 実LLMの3 fixtures × 3 runsのベンチマークは未実施。Local Modeの検証はFakeClientを用いたエンドツーエンド回帰で、実モデルの生成品質・時間・70点基準を保証しない。
- Python 3.10/3.11/3.12/LinuxのCI matrixはこのローカル検証では未実施。
- 自然言語の仕様解釈、入力モデルの正しさ、手順本文と構造化入力の意味的一致は人のレビュー対象。
- 大規模solver、日時演算、非線形Domain、Crowd接続、integration経路の自動被覆、Agile/toursは対象外。詳細は採用表を参照。
- 新しいGate閾値は未導入。変更はローカル作業ツリーに保存し、commit・push・公開・本番実行は行っていない。
